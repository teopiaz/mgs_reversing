"""OBJ → KMD/PCX/HZD/DATACNF converter modules used by tools/import_stage.py.

Each submodule is intentionally narrow: parse/write a single file format.
The CLI (../import_stage.py) wires them together for a stage build.
"""
